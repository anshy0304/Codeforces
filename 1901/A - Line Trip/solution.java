import java.util.*; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();           while (t-- > 0) {            int n = sc.nextInt();            int x = sc.nextInt();             int[] a = new int[n];            for (int i = 0; i < n; i++) {                a[i] = sc.nextInt();            }             int maxGap = 0;            int prev = 0;                         for (int i = 0; i < n; i++) {                maxGap = Math.max(maxGap, a[i] - prev);                prev = a[i];            }                         maxGap = Math.max(maxGap, x - prev);                         int lastGap = x - a[n - 1];            int ans = Math.max(maxGap, 2 * lastGap);             System.out.println(ans);        }            }}