import java.util.*; public class Main {     static boolean isValid(int x, int y){        if(x == y) return false;        if(x + y == 7) return false;        return true;    }     public static void main(String[] args){        Scanner sc = new Scanner(System.in);        int t = sc.nextInt();         while(t-- > 0){            int n = sc.nextInt();            int[] a = new int[n];             for(int i = 0; i < n; i++){                a[i] = sc.nextInt();            }             int INF = (int)1e9;            int[][] dp = new int[n][7];                         for(int x = 1; x <= 6; x++){                dp[0][x] = (a[0] == x ? 0 : 1);            }                        for(int i = 1; i < n; i++){                for(int x = 1; x <= 6; x++){                    dp[i][x] = INF;                     for(int y = 1; y <= 6; y++){                        if(isValid(x, y)){                            int cost = dp[i-1][y] + (a[i] == x ? 0 : 1);                            dp[i][x] = Math.min(dp[i][x], cost);                        }                    }                }            }                        int ans = INF;            for(int x = 1; x <= 6; x++){                ans = Math.min(ans, dp[n-1][x]);            }             System.out.println(ans);        }    }}